// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: notify.proto

/* eslint-disable */
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export const protobufPackage = "notify";

export enum NotificationChannel {
  /**
   * NOTIFICATION_CHANNEL_UNSPECIFIED - Proto3 requires a zero value; treated as "unspecified / invalid" by the
   * server (validation rejects it before reaching the handler).
   */
  NOTIFICATION_CHANNEL_UNSPECIFIED = "NOTIFICATION_CHANNEL_UNSPECIFIED",
  EMAIL = "EMAIL",
  SMS = "SMS",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export enum NotificationStatus {
  NOTIFICATION_STATUS_UNSPECIFIED = "NOTIFICATION_STATUS_UNSPECIFIED",
  PENDING = "PENDING",
  SENT = "SENT",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export interface SendNotificationRequest {
  userId: string;
  channel: NotificationChannel;
  subject: string;
  body: string;
}

export interface Recipient {
  email: string;
  displayName: string;
}

export interface Notification {
  id: string;
  userId: string;
  channel: NotificationChannel;
  subject: string;
  body: string;
  status: NotificationStatus;
  recipient?: Recipient | undefined;
  createdAt: string;
}

export const NOTIFY_PACKAGE_NAME = "notify";

/** NotifyService — private gRPC surface for the notify app. */

export interface NotifyServiceClient {
  sendNotification(request: SendNotificationRequest): Observable<Notification>;
}

/** NotifyService — private gRPC surface for the notify app. */

export interface NotifyServiceController {
  sendNotification(request: SendNotificationRequest): Promise<Notification> | Observable<Notification> | Notification;
}

export function NotifyServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["sendNotification"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("NotifyService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("NotifyService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const NOTIFY_SERVICE_NAME = "NotifyService";
